<nav>
  <ul>
    <% nav.forEach(item => { %>
      <li>
        <a href="<%= item.url %>" 
           title="<%= item.title %>"
           class="nav-link">
          <%= item.name %>
        </a>
      </li>
    <% }); %>
  </ul>
</nav>
